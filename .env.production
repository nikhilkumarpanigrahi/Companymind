# =============================================================================
# CompanyMind — Production Environment Variables
# =============================================================================
# Fill in your production values before deploying

# Frontend (baked into build at compile time via Vite)
VITE_API_BASE_URL=

# Backend Server
PORT=8080
NODE_ENV=production

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://companymind_app:Epsteinfiles123@companymind-dev.3mmcu3h.mongodb.net/companymind?retryWrites=true&w=majority
MONGODB_DB_NAME=companymind

# Embedding Service — in Docker Compose, use the service name as hostname
EMBEDDING_API_URL=http://embedding:8000/embed-query
EMBEDDING_API_KEY=

# Vector Search Index Name
VECTOR_INDEX_NAME=documents_embedding_index

# Groq API Key
GROQ_API_KEY=
